# RBM-Matlab

Restricted Boltzmann Machine implementation in MATLAB on Bars-as-Stripes dataset. 

Using Batch gradient ascent doesn't perform as well as stochastic gradient ascent (when taking every training example into account)
